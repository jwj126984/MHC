<Window x:Class="MHC.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="密码验证" Height="220" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        WindowStyle="SingleBorderWindow"
        SizeToContent="Height">
    <Grid Margin="20,15,20,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="请输入密码" FontSize="14" FontWeight="Bold" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" x:Name="PasswordContainer" LastChildFill="True">
                <PasswordBox x:Name="PasswordInput" Height="30" VerticalContentAlignment="Center"
                             PasswordChanged="PasswordInput_PasswordChanged"/>
            </DockPanel>

            <Button Grid.Column="1" x:Name="ToggleVisibilityButton" Content="显示"
                    Width="60" Height="30" Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Click="ToggleVisibilityButton_Click"/>
        </Grid>

        <TextBlock Grid.Row="2" x:Name="HintMessage" Text="提示：点击“显示”可查看当前输入"
                   Foreground="Gray" FontSize="11" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <TextBlock Grid.Row="3" x:Name="ErrorMessage" Text="" Foreground="Red" FontSize="12"
                   Margin="0,0,0,10" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OKButton" Content="确定" Width="80" Height="30" Margin="0,0,10,0" IsDefault="True" Click="OKButton_Click"/>
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="30" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
