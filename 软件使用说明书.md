# 碰撞电源模块监控系统使用说明书

**软件版本**: V1.07
**编制日期**: 2026-09-09

---

## 目录

1. [软件概述](#1-软件概述)
2. [系统要求](#2-系统要求)
3. [安装与启动](#3-安装与启动)
4. [CAN 配置与连接](#4-can-配置与连接)
5. [界面总览](#5-界面总览)
6. [监控区功能说明](#6-监控区功能说明)
7. [控制区功能说明](#7-控制区功能说明)
8. [故障监控区功能说明](#8-故障监控区功能说明)
9. [参数设置（密码保护）](#9-参数设置密码保护)
10. [日志与数据记录](#10-日志与数据记录)
11. [指示灯与状态显示说明](#11-指示灯与状态显示说明)
12. [通信协议说明](#12-通信协议说明)
13. [常见问题排查](#13-常见问题排查)
14. [技术支持](#14-技术支持)

---

## 1. 软件概述

碰撞电源模块监控系统是一款用于监控超级电容模组运行状态的专用上位机软件。软件通过 **CAN 总线**与目标设备（超级电容模组控制器）通信，实时采集并展示电容模组的各项参数、充放电状态、故障信息，并支持对门锁电机、充放电、测试工况、休眠唤醒等功能的远程控制。

**软件名称**: 碰撞电源模块监控系统
**软件版本**: V1.07
**主要功能**:
- 超级电容模组状态实时监控（5 个电容单体 + 模组综合 + 告警）
- 门锁电机、驱动通道、电源、碰撞信号等外围状态监控
- 门锁点控、充放电控制、自动化测试工况控制
- 休眠/唤醒控制与版本号读取（UDS 诊断）
- 故障自动检测、记录与故障码读取（UDS 协议）
- CAN 报文与故障日志自动保存

---

## 2. 系统要求

### 2.1 硬件要求

| 项目 | 要求 |
|------|------|
| 主机 | PC 或工控机一台 |
| CAN 设备 | 周立功（ZLG）USBCANFD-200U 或 USBCANFD-100U |
| 通信线缆 | CAN 通信线缆（CANH/CANL/地线），建议双绞屏蔽线 |
| 目标设备 | 超级电容模组控制器（具备 CAN 通信接口） |

> 说明：软件底层兼容多种周立功 CAN 设备（USBCAN1/2、USBCAN-E-U 系列、USBCANFD 系列、PCIE-CANFD 系列、CANET 系列、CANFDNET/CANFDWIFI 系列等），当前版本配置窗口下拉列表中提供 **USBCANFD-200U** 和 **USBCANFD-100U** 两种可选设备。

### 2.2 软件要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / Windows 11（x64） |
| 运行环境 | .NET 10.0 Runtime |
| 必要驱动 | 对应 CAN 设备的驱动及 ZLG 二次开发库（随安装包分发） |

---

## 3. 安装与启动

1. 运行安装程序（由 Inno Setup 生成的 `碰撞电源x64` 安装包），按向导提示完成安装；
2. 安装完成后可从桌面快捷方式或开始菜单启动软件；
3. 程序启动后首先弹出 **CAN 配置窗口**（详见第 4 章），配置成功后进入主界面；若点击"取消"，程序将直接退出。

**启动流程图**：

```
启动程序
   │
   ▼
CAN配置窗口 ──取消──▶ 退出程序
   │
   连接成功
   ▼
主界面（监控区 / 控制区 / 故障监控）
```

---

## 4. CAN 配置与连接

程序启动时会自动弹出"CAN 设备配置"窗口，用于建立与目标设备的 CAN 通信连接。

### 4.1 配置参数

| 参数 | 可选值 | 说明 |
|------|--------|------|
| 设备类型 | USBCANFD-200U / USBCANFD-100U | 按实际接入的 CAN 设备型号选择 |
| 设备索引号 | 0 ~ 3 | 同一电脑接入多台设备时用于区分，单台设备选择 0 |
| 通道号 | 0 ~ 3 | CAN 通道选择，通常使用通道 0 |
| 波特率 | 5k ~ 1M（默认 500k） | 须与目标设备波特率一致，默认选中 500 kbps |

可用波特率：1 Mbps、800 kbps、500 kbps、250 kbps、125 kbps、100 kbps、50 kbps、20 kbps、10 kbps、5 kbps。

### 4.2 连接操作

1. 依次选择**设备类型、设备索引号、通道号、波特率**；
2. 点击 **"连接"** 按钮，窗口下方显示连接进度与结果：
   - `正在连接...`（蓝色）→ 连接中；
   - `连接成功`（绿色）→ 进入主界面；
   - `打开设备失败 / 初始化通道失败 / 启动CAN失败`（红色）→ 按提示检查设备后重试；
3. 点击 **"取消"** 关闭窗口并退出程序。

### 4.3 连接注意事项

- **CAN FD 设备**（USBCANFD 系列）连接时，软件自动将**仲裁段波特率**设置为所选波特率、**数据段波特率**固定为 2 Mbps，并**自动开启终端电阻**；
- 确认 CAN 线连接正确（CANH↔CANH、CANL↔CANL），且总线两端终端电阻匹配；
- 连接成功后主界面顶部监控区开始实时显示接收数据。

---

## 5. 界面总览

主界面采用 **Tab 页签**设计，包含三个主要功能区域：

```
┌────────────────────────────────────────────────────────────┐
│  碰撞电源模块监控系统V1.07                                   │
├────────────────────────────────────────────────────────────┤
│  [ 监控区 ]  [ 控制区 ]  [ 故障监控 ]                        │
├────────────────────────────────────────────────────────────┤
│ 监控区：电容监控 / SRS碰撞信号 / 模组告警 / 唤醒源           │
│        电源监控 / ARM碰撞信号 / 驱动通道保护 / 电机 / 内部   │
│        顶栏含[参数设置][清零][故障次数]                      │
│ 控制区：点控功能 / 充放电控制 / 自动化控制 / 参数设置        │
│        休眠唤醒控制 / 版本号读取                             │
│ 故障监控：故障码读取（UDS协议）                              │
└────────────────────────────────────────────────────────────┘
```

---

## 6. 监控区功能说明

监控区是主界面默认页签，实时显示所有采集到的信号数据。**顶栏**包含：

| 控件 | 功能 |
|------|------|
| 参数设置按钮 | 打开参数设置窗口（需密码验证，见第 9 章） |
| 清零按钮 | 将故障计数清零 |
| 故障次数 | 显示本次运行累计记录的故障次数（红色加粗） |

### 6.1 电容监控

**（1）电容单体监控**（5 个单体）

| 监控项 | 说明 |
|--------|------|
| 单体 | Cell1 ~ Cell5 |
| 电压(mV) | 各单体电容的实时电压值 |
| 过压 | 正常（绿色）/ 故障（红色） |
| 短路 | 正常（绿色）/ 故障（红色） |
| 均衡 | 是（绿色）/ 否（灰色→无数据） |
| 均衡次数 | 该单体均衡动作发生的累计次数 |

**（2）电容模组综合监控**

| 监控项 | 说明 |
|--------|------|
| 模组电量(%) | 超级电容模组的当前电量百分比 |
| 模组电压(mV) | 模组总电压 |
| 模组电流(mA) | 实时电流 |
| 放电平均电流(mA) | 放电过程的平均电流 |
| 充放电状态 | 正常 / 充电状态 / 放电状态 / 故障模式（颜色见第 11 章） |

### 6.2 SRS 碰撞 CAN 信号监控

| 监控项 | 说明 |
|--------|------|
| 碰撞信号 | 正常 / PVDD过压 / 欠压 / VCP欠压 / SPI时钟异常 |

### 6.3 模组告警监控

| 监控项 | 说明 |
|--------|------|
| 温度(°C) | 模组当前温度 |
| 过温 | 正常 / 过温故障 |
| 容值(F) | 模组当前容值（保留 1 位小数） |
| 容值状态 | 正常 / 异常（超出容值上下限阈值时告警） |
| 内阻(mΩ) | 模组内阻值 |
| 内阻状态 | 正常 / 异常（超过内阻门限时告警） |
| 断路状态 | 正常 / 断路故障 |
| 不均衡 | 正常 / 不均衡告警（单体电压差超限时告警） |
| 充电故障 | 正常 / 充电故障 |
| 放电故障 | 正常 / 放电故障 |

### 6.4 唤醒源监控

| 监控项 | 说明 |
|--------|------|
| CAN 唤醒 | 显示 CAN 唤醒信号状态 |
| KL15 唤醒 | 显示 KL15 唤醒信号状态 |

### 6.5 电源监控

| 监控项 | 说明 |
|--------|------|
| KL30 电压(mV) | 常电（30 号线）电压 |
| KL30 故障状态 | 正常 / 故障 |
| KL15 电压(mV) | 点火电（15 号线）电压 |
| KL15 故障状态 | 正常 / 故障 |

### 6.6 ARM 碰撞信号监控

| 监控项 | 说明 |
|--------|------|
| PWM 占空比(%) | ARM 输出 PWM 信号占空比 |
| PWM 状态 | 正常 / 故障 |

### 6.7 驱动通道保护监控

监控 10 个驱动通道的 **VDS（漏源电压保护）** 与 **VGS（栅源电压保护）** 状态：

| 通道名称 | 说明 |
|----------|------|
| LFCenterM+ | 左前中心电机正极 |
| LRCenterM+ | 左后中心电机正极 |
| LFCenterM- | 左前中心电机负极 |
| LRMMinus | 左后电机负极 |
| LRChildM+ | 左后儿童锁电机正极 |
| RFCenterM- | 右前中心电机负极 |
| RFCenterM+ | 右前中心电机正极 |
| RRMMinus | 右后电机负极 |
| RRCenterM+ | 右后中心电机正极 |
| RRChildM+ | 右后儿童锁电机正极 |

每个通道显示 VDS 与 VGS 两项状态：正常（绿色）/ 故障（红色）。

### 6.8 电机监控

监控 6 个电机的 3 种故障状态：

| 电机名称 | 说明 |
|----------|------|
| 电机1（LF 前门） | 左前门锁电机 |
| 电机2（RF 前门） | 右前门锁电机 |
| 电机3（LR 后门） | 左后门锁电机 |
| 电机4（RR 后门） | 右后门锁电机 |
| 电机5（LR 儿童锁） | 左后儿童锁电机 |
| 电机6（RR 儿童锁） | 右后儿童锁电机 |

每个电机监控三种故障类型：
- **短电源**：电机线路对电源短路
- **短地**：电机线路对地短路
- **开路**：电机线路断路

### 6.9 内部监控

显示控制器内部驱动状态，为位组合状态。状态字各位含义：

| 位 | 含义 | 位 | 含义 |
|----|------|----|------|
| bit0 | SPI 错误 | bit8 | PVDD 欠压 |
| bit1 | SPI 时钟错误 | bit9 | PVDD 过压 |
| bit2 | 上电复位 | bit10 | VCP 欠压 |
| bit3 | nFAULT 引脚故障 | bit11 | 过温警告 |
| bit4 | 警告指示 | bit12 | 过温关断 |
| bit5 | DS/GS 故障 | bit13 | 看门狗故障 |
| bit6 | 欠压故障 | bit14 | 复合警告 |
| bit7 | 过压故障 | | |

状态为 0 时显示"正常"（绿色）；非 0 时按位显示具体故障项（红色）。

### 6.10 CAN 通信统计

| 统计项 | 说明 |
|--------|------|
| 发送帧 | 软件已发送的 CAN 帧总数 |
| 接收帧 | 软件已接收的 CAN 帧总数 |
| 错误帧 | 通信异常帧/处理异常累计数 |

---

## 7. 控制区功能说明

### 7.1 点控功能

通过按钮手动控制门锁状态。**点击一次点亮（绿色）＝下发使能，再次点击熄灭＝下发解除**。

| 按钮 | 功能 |
|------|------|
| 左前门锁解锁 | 控制左前门锁解锁 |
| 右前门锁解锁 | 控制右前门锁解锁 |
| 左后门锁解锁 | 控制左后门锁解锁 |
| 右后门锁解锁 | 控制右后门锁解锁 |
| 左后门儿童锁 | 控制左后门儿童锁 |
| 右后门儿童锁 | 控制右后门儿童锁 |

> 任意点控按钮点亮时，设备进入"点控工况"（工况号 7）；全部点控解除后恢复默认工况（工况号 0）。

### 7.2 充放电控制

| 按钮 | 功能 |
|------|------|
| 充电 | 切换充电使能/停止（绿色点亮＝充电中） |
| 放电 | 切换放电使能/停止（绿色点亮＝放电中） |

> 充电与放电互斥：启动充电会自动解除放电，反之亦然。

### 7.3 自动化控制

用于执行预设测试工况，三种工况互斥，同一时刻只允许一种执行。点击按钮开始（按钮变绿、状态显示"执行中"红色），再次点击停止（状态恢复"停止"绿色）。

| 按钮 | 工况号 | 状态显示 |
|------|--------|----------|
| DV 环境工况 | 1 | 执行中 / 停止 |
| DV 热测试工况 | 2 | 执行中 / 停止 |
| EMC 试验工况 | 3 | 执行中 / 停止 |

> 状态显示除受按钮控制外，还会根据设备回传的工况状态信号（DVNormalStatus / DVHotStatus / EMCStatus）自动刷新，1＝执行中，0＝停止。

### 7.4 参数设置（循环参数）

| 参数 | 说明 | 默认值 | 设置方式 |
|------|------|--------|----------|
| 已循环次数 | 设备回传的当前已完成循环数 | 只读 | — |
| 设置循环次数 | 目标循环总数（0~65535） | 100 | 输入后点击"设置" |
| 循环间隔时间 | 每次循环之间的间隔 | 1000 ms | 输入后点击"设置" |

> 间隔时间换算：软件将输入的毫秒值除以 5 作为 delay 参数下发给设备；输入 0 时下发 0（由设备端按默认 10 秒执行）。

### 7.5 休眠唤醒控制

| 功能 | 说明 |
|------|------|
| 当前状态显示 | 实时显示设备状态：`当前状态: 唤醒`（绿色）/ `当前状态: 休眠`（红色） |
| 进入休眠 | 发送休眠指令，使设备进入休眠模式 |
| 退出休眠 | 发送唤醒指令，使设备退出休眠 |
| CAN 唤醒 | 周期性（10 ms）发送 CAN 唤醒报文（ID 0x400），保持总线唤醒。点击开启（按钮变"停止唤醒"、状态"运行中"绿色），再次点击停止 |

> 休眠状态显示由设备回传信号（SleepStatus）自动更新：1＝休眠，0＝唤醒。

### 7.6 版本号读取

| 功能 | 说明 |
|------|------|
| 读取版本号 | 通过 UDS 诊断服务（0x22）读取设备软件与硬件版本 |
| 软件版本 | 显示读取到的软件版本号 |
| 硬件版本 | 显示读取到的硬件版本号 |

点击"读取版本号"后，软件发送 UDS 读取请求（DID：软件版本 0xF189、硬件版本 0xF089），收到响应后显示对应版本；未收到响应时显示"读取失败"，CAN 未连接时显示"CAN未连接"。

---

## 8. 故障监控区功能说明

### 8.1 故障码读取（UDS 协议）

1. 点击 **"读取故障码"** 按钮；
2. 软件通过 CAN 发送 UDS 诊断请求（响应 ID 0x710）；
3. 故障码列表显示读取结果（收到响应前显示"读取中..."；CAN 未连接时显示"CAN未连接"）。

### 8.2 故障自动检测与记录

软件在数据刷新过程中自动检测以下故障，检测到**首次发生**时自动记录到故障日志（重复故障不重复记录，故障恢复后再次发生可重新记录）：

| 类别 | 故障项 |
|------|--------|
| 电容单体 | Cell1~Cell5 过压、短路 |
| 电容模组 | 过温、容值异常（超出上下限）、内阻异常（超门限）、断路、不均衡（单体电压差超限）、充电故障、放电故障 |
| 电源 | KL30 故障、KL15 故障 |
| 碰撞信号 | 碰撞信号故障 |
| 电机 | 6 个电机的短电源、短地、开路故障 |
| 驱动通道保护 | 10 个通道的 VDS / VGS 保护触发 |
| 内部驱动 | 内部状态字非 0（按位解析具体故障） |
| 通信 | CAN 连接断开 |

每次故障发生时，日志会记录**故障发生时刻所有信号的完整快照**（见第 10 章）。

---

## 9. 参数设置（密码保护）

### 9.1 入口与密码验证

1. 点击监控区顶栏 **"参数设置"** 按钮；
2. 弹出 **密码验证** 窗口，输入密码后点击"确定"；
3. 密码正确 → 打开参数设置窗口；密码错误 → 提示"密码错误，请重新输入"并保持窗口打开。

**初始密码**: `admin`（首次使用后请及时在参数设置中修改）。

> 密码验证窗口支持"显示/隐藏"切换明文，方便核对输入。

### 9.2 可设置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 容值最小值(F) | 5.0 | 容值低于此值判为异常 |
| 容值最大值(F) | 6.0 | 容值高于此值判为异常 |
| 均衡电压差(V) | 0.12 | 单体间最大允许电压差，超过判为不均衡 |
| 内阻门限(mΩ) | 200 | 内阻超过此值判为异常 |
| 过温阈值(°C) | 85 | 温度超过此值判为过温 |
| 放电电流(mA) | -10000 | 放电状态下电流低于此值（负值方向）判为放电故障 |

### 9.3 操作步骤

1. 在参数设置窗口中修改各输入框数值；
2. 点击 **"保存参数"** 保存并关闭窗口，弹出"参数保存成功！"提示；
3. 输入无效时窗口内红色文字提示具体错误项，修改后重试；
4. 点击 **"取消"** 放弃修改并关闭。

### 9.4 修改密码

1. 在参数设置窗口"新密码"输入框输入新密码（**至少 4 位**）；
2. 点击 **"修改密码"**，立即生效并弹出"密码修改成功！"提示。

### 9.5 存储位置

| 文件 | 位置 | 内容 |
|------|------|------|
| settings.ini | `%APPDATA%\MHC\` | 上述 6 项参数阈值 |
| password.ini | `%APPDATA%\MHC\` | 密码的 SHA-256 哈希值 |

---

## 10. 日志与数据记录

软件自动记录两类日志，均无需手动开启。

### 10.1 CAN 报文日志（CSV）

- **位置**：程序目录 `Logs\yyyy-MM-dd\` 子目录下；
- **文件名**：`HH-mm-ss.csv`（以文件创建时刻命名）；
- **分割规则**：每 **10 分钟**自动生成新文件；
- **列格式**：

| 列名 | 说明 |
|------|------|
| Time | 时间（HH:mm:ss.fff） |
| Direction | 发送 / 接收 |
| FrameId | 帧 ID（十六进制） |
| FrameType | 标准帧 / 扩展帧 |
| ProtocolType | CAN / CAN FD |
| DataLength | 数据长度（字节） |
| Data | 数据字节（十六进制，空格分隔） |
| Status | 状态（正常） |

### 10.2 故障日志（Excel）

- **位置**：程序目录 `Logs\` 目录下；
- **文件名**：`yyyy-MM-dd.xlsx`（每天一个文件）；
- **内容**：每行记录一次故障发生，首列为"故障发生时间"，其余列为**故障发生瞬间所有监控信号的显示名与数值快照**；
- **样式**：发生故障的信号列以**红色字体**标出；
- **去重规则**：同一故障在未恢复前只记录一次，恢复后再次发生才重新记录。

---

## 11. 指示灯与状态显示说明

### 11.1 颜色含义

| 颜色 | 含义 |
|------|------|
| 绿色 | 正常 / 无故障 / 功能使能 / 执行中 |
| 红色 | 故障 / 告警 / 测试执行中 / 休眠状态 |
| 橙色 | 充电中 / 放电中（充放电状态显示） |
| 灰色 | 无数据（未收到对应报文）/ 功能未启用 |

### 11.2 充放电状态

| 显示值 | 含义 |
|--------|------|
| 正常 | 未处于充放电 |
| 充电状态 | 设备正在充电 |
| 放电状态 | 设备正在放电 |
| 故障模式 | 设备处于故障模式 |

### 11.3 碰撞信号状态（SRS / PWM 状态）

| 显示值 | 含义 |
|--------|------|
| 正常 | 无异常 |
| PVDD 过压 | 电源电压过高 |
| 欠压 | 电源欠压 |
| VCP 欠压 | 电荷泵电压欠压 |
| SPI 时钟异常 | SPI 通信时钟异常 |

---

## 12. 通信协议说明

| 报文 | CAN ID | 类型 | 方向 | 说明 |
|------|--------|------|------|------|
| 监控报文 | 0x600 (1536) | CAN FD | 接收 | 电容模组状态信号（单体电压、告警、电机、通道保护等） |
| 控制命令 | 0x601 (1537) | CAN FD | 发送 | 点控/充放电/休眠/工况/循环参数 |
| 版本读取 | 0x718 | 标准帧 | 发送 | UDS 0x22 读取 DID 0xF189（软件）/ 0xF089（硬件） |
| UDS 响应 | 0x710 | 标准帧 | 接收 | 版本号等诊断响应 |
| CAN 唤醒 | 0x400 | 标准帧 | 发送 | 周期 10ms 的唤醒报文 |

**控制命令数据位定义（0x601）**：

| 位 | 含义 |
|----|------|
| bit15 | 休眠指令 |
| bit14 | 充电使能 |
| bit13 | 放电使能 |
| bit10~8 | 工况号（0=默认/停止，1=DV环境，2=DV热，3=EMC，7=点控） |
| bit7~4 | 左前门 / 右前门 / 左后门 / 右后门使能 |
| bit3~2 | 左后儿童锁 / 右后儿童锁 |
| Data[2:3] | 循环间隔 delay 参数（输入毫秒值 ÷ 5，0 表示设备端默认 10s） |
| Data[4:5] | 循环次数设置 |

---

## 13. 常见问题排查

### 13.1 CAN 连接失败

- 检查 CAN 设备是否正确插入 USB/PCIe 接口，驱动是否安装；
- 确认设备类型、设备索引号选择与实际设备一致；
- 检查波特率设置是否与目标设备一致；
- 确认 CANH/CANL 接线是否正确、总线两端终端电阻是否匹配；
- 重新插拔设备后重试。

### 13.2 数据不更新（监控区显示灰色"无"）

- 确认 CAN 通信正常（观察监控区底部"接收帧"计数是否持续增长）；
- 检查目标设备是否上电、是否处于休眠状态（休眠时无报文，可点击"退出休眠"或"CAN 唤醒"）；
- 确认总线波特率匹配。

### 13.3 频繁弹出 CAN 错误提示

- 软件在 5 秒内连续收到 3 次以上 CAN 错误时会弹出提示；
- 检查线缆连接、屏蔽接地、终端电阻，排除总线干扰；
- 检查波特率是否稳定匹配。

### 13.4 故障持续告警

- 点击监控区顶栏"清零"可清除故障计数；
- 确认实际硬件状态（是否真实存在过压/短路/过温等）；
- 必要时断电重启目标设备；
- 查看 `Logs\yyyy-MM-dd.xlsx` 故障日志中的信号快照辅助定位。

### 13.5 忘记参数设置密码

- 联系软件维护人员处理（密码以哈希形式保存在 `%APPDATA%\MHC\password.ini`，可删除该文件后恢复默认密码 `admin`）。

---

## 14. 技术支持

如遇问题，请联系技术支持人员，并提供以下信息：
- 软件版本号（窗口标题或控制区"读取版本号"结果）
- CAN 设备型号与波特率配置
- 故障现象描述与复现步骤
- 相关日志文件（`Logs\` 目录下的 CSV / xlsx 文件）

---

**版本记录**

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-04-30 | 初版说明书 |
| V1.07 | 2026-09-09 | 依据 V1.07 版本软件同步更新：新增参数设置（密码保护）、CAN 唤醒、报文 CSV 日志、故障 Excel 日志等说明 |
